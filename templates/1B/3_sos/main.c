/*
SOS
Blinks the built-in LED in an SOS signal
Three shorts -- three longs --- three shorts -- delay, also known as ...---...
This requires a lot of delay lengths, so its best to set them up as constants
and give them names to make them easier to remember
*/
 
// Pin 13 has an LED connected on most Arduino boards.

int main() {

while(1) {
    }
    return(0);
}
